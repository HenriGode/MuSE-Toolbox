# @package _global_
defaults:
  - override /dataset: brudex_main

# Example: Changing the signal_lengths and batch size for this specific experiment
dataset:
  id: "J3_BXLS_test"
  generation_config:
    signal_lengths: 60.0 # [s]
    fix_seglen: False    

  batch_size: 10

logger:
  project: "J3_test"
  name: "J3_BXLS_test_exp"

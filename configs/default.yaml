defaults:
  - dataset: brudex
  - model: cosad
  - transform: default
  - trainer: default
  - logger: wandb
  - callbacks: default
  - task: null
  - _self_
  - experiment: J3_brudex

# Global settings
task: source_counting
experiment_name: default_exp
train: True
test: True
predict: False
predictions_dir: null

paths:
  root_dir: ${oc.env:PROJECT_ROOT}
  data_dir: ${paths.root_dir}/data

hydra:
  run:
    dir: ${paths.root_dir}/outputs/${task}/${experiment_name}/${logger.name}/${now:%Y-%m-%d_%H-%M-%S}
  sweep:
    dir: ${paths.root_dir}/outputs/${task}/${experiment_name}/multirun/${logger.name}
    subdir: ${hydra.job.num}
  output_subdir: logging/.hydra
